export enum GameStateType {
  WAIT = 'wait',
  CHOOSE = 'choose',
  RESTART = 'restart',
  PLAY = 'play',
  OVER = 'over',
}

export enum GamePreviewState {
  PICK = 'pick',
  WAIT = 'wait',
  SHOW = 'show',
}
